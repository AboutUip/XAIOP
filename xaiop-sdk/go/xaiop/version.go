// Package xaiop is the official Go product SDK for XAIOP (protocol wire v0.7.0 Draft).
package xaiop

// ProtocolVersion is the wire package this SDK targets.
const ProtocolVersion = "0.7.0"

// SDKVersion is the Go module product version.
const SDKVersion = "0.16.0"
