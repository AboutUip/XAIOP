// Package xaiop is the Go SDK for the XAIOP core protocol track (wire v0.6.0 Frozen).
package xaiop

// ProtocolVersion is the Frozen wire package this track targets.
const ProtocolVersion = "0.6.0"

// SDKVersion is the Go module version for the core-protocol track.
const SDKVersion = "0.6.0-alpha.2"
