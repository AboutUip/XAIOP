// Package xaiop is the official Go product SDK for XAIOP (protocol wire v0.6.0 Frozen).
package xaiop

// ProtocolVersion is the Frozen wire package this SDK targets.
const ProtocolVersion = "0.6.0"

// SDKVersion is the Go module product version.
const SDKVersion = "0.15.1"
