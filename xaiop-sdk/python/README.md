# XAIOP Python SDK

Installable package `xaiop` for XAIOP v0.1.0.

## Status

**Pending update** — scaffold only. Implementation waits until the **Node.js** SDK lands and freezes API shape.

See [../README.md](../README.md).

## Setup (scaffold)

```bash
python -m pip install -e ".[dev]"
pytest
```

## Layout

```text
src/xaiop/
tests/
```
