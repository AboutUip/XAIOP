# XAIOP Java SDK

Maven project targeting a publishable **JAR** (`io.xaiop:xaiop`) for XAIOP v0.1.0.

## Status

**Pending update** — scaffold only. Implementation waits until the **Node.js** SDK lands and freezes API shape.

See [../README.md](../README.md).

## Build (scaffold)

```bash
mvn -q -DskipTests package
```

Artifact: `target/xaiop-0.1.0.jar`

## Layout

```text
src/main/java/io/xaiop/
src/test/java/io/xaiop/
```
